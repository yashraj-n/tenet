{
  "name": "aura-ai-agent",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "server:dev": "cd apps/server && bun dev",
    "server:db:generate": "cd apps/server && bun db:generate",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check"
  },
  "devDependencies": {
    "oxfmt": "^0.57.0",
    "oxlint": "^1.72.0"
  }
}
